# CLK 2.6.0 Structural Slimming Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Reduce the canonical installed CLK Skill to a compact executable kernel without changing any method behavior or machine contract.

**Architecture:** Keep `chain-loop-skill/SKILL.md` as the always-loaded kernel and route detailed procedures to the ten existing canonical references. Lock the compact boundary with repository tests; leave Python, schemas, templates, and references behaviorally unchanged.

**Tech Stack:** Markdown, Python, pytest, Git integrity manifests.

---

### Task 1: Lock the slim Skill contract with a failing test

**Files:**
- Modify: `tests/test_clk_contract.py`
- Test: `tests/test_clk_contract.py`

- [ ] **Step 1: Add the compact-kernel assertions**

Add a test that reads `chain-loop-skill/SKILL.md`, requires at most 650 physical
lines, requires the kernel headings `Canonical Identity`, `Method Selection Gate`,
`Hard Rules`, `Authority`, `Execution`, `Runtime Safeguards`, `Evidence and
Recovery`, and `Launch Checklist`, and requires links to all ten reference files
listed by the accepted design.

- [ ] **Step 2: Run the test and verify RED**

Run: `python -m pytest -q tests/test_clk_contract.py::test_canonical_skill_is_a_compact_routed_kernel`

Expected: FAIL because the current canonical Skill has 1000 physical lines.

- [ ] **Step 3: Commit the RED contract**

Run: stage only `tests/test_clk_contract.py` and commit with
`test: lock compact CLK skill kernel`.

### Task 2: Replace repeated prose with the routed kernel

**Files:**
- Modify: `chain-loop-skill/SKILL.md`

- [ ] **Step 1: Preserve the invariant inventory**

Before rewriting, record every current heading, hard rule, authority owner, gate,
route, safeguard, final condition, and linked canonical reference.

- [ ] **Step 2: Write the compact kernel**

Retain the canonical identity and one ordered operating path. Keep all 24 hard-rule
meanings, role authority, method selection, Calabash/readiness/simulation gates,
Level barrier, direct Verification route, autonomy, model/wake/Patrol/progress/
capacity/Pin policy, evidence/recovery boundary, final audit, and publication gate.
Replace duplicated record fields and procedure enumerations with direct links to
their existing references.

- [ ] **Step 3: Run the focused test and verify GREEN**

Run: `python -m pytest -q tests/test_clk_contract.py::test_canonical_skill_is_a_compact_routed_kernel`

Expected: PASS with the Skill at 650 lines or fewer.

- [ ] **Step 4: Run all contract and repository tests**

Run: `python -m pytest -q tests/test_clk_contract.py tests/test_repository_validation.py tests/test_contract.py`

Expected: all tests and subtests pass.

### Task 3: Refresh integrity evidence and verify behavior

**Files:**
- Modify: `FILE_HASHES.json`
- Modify: `VALIDATION-REPORT.md`

- [ ] **Step 1: Refresh the validation report**

Record the new Skill line count, unchanged production Python surface, focused tests,
full tests, optimized tests, all validators, and preserved method boundaries.

- [ ] **Step 2: Refresh deterministic hashes**

Run: `python scripts/update_file_hashes.py`

Expected: `FILE_HASHES.json` covers the final release tree.

- [ ] **Step 3: Run all repository validators**

Run the repository, chain-level plan, runtime state, topology fault, run control,
model policy, Receipt chain, and readiness commands listed in `README.md`.

Expected: every validator reports PASS.

- [ ] **Step 4: Run optimized and full tests**

Run: `python -O -m pytest -q tests/test_model_policy.py tests/test_run_control.py`

Run: `python -m pytest -q`

Expected: zero failures.

- [ ] **Step 5: Commit the verified slimming candidate**

Run: `git diff --check`, stage only confirmed files, and commit with
`refactor: slim CLK skill entrypoint`.
