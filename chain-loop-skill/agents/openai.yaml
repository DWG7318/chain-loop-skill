interface:
  display_name: "Chain Loop Skill (CLK)"
  short_description: "Fixed multi-Chain Runs with full Level barriers"
  default_prompt: "Use only $chain-loop-skill (CLK), exactly once. Preserve fixed persistent Chains, ordered Levels, full Barriers, immutable Receipts, Worker-owned rework, and fresh D2/LEVEL/D3 Verification. Freeze Calabash, the maximum valid Chain roster, versioned Required sets, DEVICE_CAPACITY_PROFILE, CUMULATIVE_ENGINEERING_LOAD, and dispatch only CELL_CAPACITY_GATE PASS work. Worker alone may use the bounded T+0/T+2/T+4/T+6 ladder to wake its frozen Checker with GO/CELL n/N delivery scope; Checker derives D1 progress, while Supervisor reports only D2/Level/Run milestones and never waits with wait_threads. Create exactly one non-authoritative gpt-5.6-luna+xhigh Run patrol and heartbeat. Treat GO/CELL/Round as subtasks and visible same-project tasks as peer roles; forbid spawn_agent, delegate_task, hidden/background Agents. Deny set_thread_pinned to every method role; only explicit Owner provenance legitimizes Pin. Preserve topology fault localization and never load, combine, borrow, or switch to SLK or GLK inside the Run."
